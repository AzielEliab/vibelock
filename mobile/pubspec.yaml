name: vibelock
description: Local-first VibeLock mic client. Energy/ZCR authenticity advisory, not courtroom proof.
publish_to: "none"
version: 0.3.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  record: ^5.2.1
  path_provider: ^2.1.5
  file_picker: ^8.1.4
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
